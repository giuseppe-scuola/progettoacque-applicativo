void Grafico(record Array[LunghezzaDatabase], unsigned int IndiceVuoto)
{
    #if defined(_WIN32) || defined(_WIN64)
    //Codice qui
    #else
    cout << "Funzione non supportata su questo sistema operativo..." << endl;
    #endif
}
